FROM ubuntu:20.04

ARG DEBIAN_FRONTEND=noninteractive
ENV TZ="Europe/Paris"

USER 0

RUN apt-get update && apt-get upgrade -y && apt-get install apache2 wget vim htop zip unzip -y --no-install-recommends 

EXPOSE 8080 80

USER 1000990000

CMD ["apache2foreground"]
